{
  "name": "prompt-injection",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "demo": "tsx src/main.ts --offline --attack all --defense all --trials 1",
    "online": "tsx src/main.ts",
    "check": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run --exclude 'out/**'"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "openai": "^4.96.0"
  }
}
